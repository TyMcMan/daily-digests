// One entry per digest, ISO date, ascending. The daily agent appends exactly one line per run.
// If this file is ever corrupted, rebuild it:  ls 20*.html | sed 's/\.html//' | sort  → re-wrap as the array below.
window.DIGEST_DATES = [
];
